import { FilterBorderColorPipe } from './filter-border-color.pipe';

describe('FilterBorderColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBorderColorPipe();
    expect(pipe).toBeTruthy();
  });
});
